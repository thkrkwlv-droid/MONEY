name: Keep MONEY Render Awake

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping MONEY Backend Health
        run: |
          curl -L --fail --show-error --silent https://money-83qs.onrender.com/api/health > /dev/null
